name: Build APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'
          channel: 'stable'
          cache: true

      - name: Test dependencies (our pubspec + default lib)
        run: |
          set -o pipefail
          cd /tmp
          flutter create --org com.novelwriter test_deps
          cd test_deps
          cp $GITHUB_WORKSPACE/pubspec.yaml .
          flutter pub get 2>&1
          echo "=== Building with our deps + default lib ==="
          flutter build apk --release 2>&1 | tee /tmp/test_deps.log
          EXIT_CODE=${PIPESTATUS[0]}
          if [ $EXIT_CODE -ne 0 ]; then
            echo "=== DEPS TEST FAILED (problem is in dependencies) ==="
            tail -100 /tmp/test_deps.log
          else
            echo "=== DEPS TEST PASSED (problem is in Dart code) ==="
          fi
          echo "DEPS_EXIT=$EXIT_CODE" >> $GITHUB_OUTPUT
        continue-on-error: true

      - name: Build APK
        run: |
          set -o pipefail
          
          cd /tmp
          flutter create --org com.novelwriter --project-name novel_writer fresh_app
          cd fresh_app
          
          rm -rf lib
          cp -r $GITHUB_WORKSPACE/lib .
          cp $GITHUB_WORKSPACE/pubspec.yaml .
          
          flutter pub get 2>&1
          
          flutter build apk --release 2>&1 | tee build.log
          EXIT_CODE=${PIPESTATUS[0]}
          
          if [ $EXIT_CODE -ne 0 ]; then
            echo "=== BUILD FAILED - tail of log ==="
            tail -200 build.log
            exit $EXIT_CODE
          fi
          
          echo "=== BUILD SUCCEEDED ==="
          find . -name "*.apk" 2>/dev/null
          
          APK=$(find . -name "*.apk" 2>/dev/null | head -1)
          if [ -n "$APK" ]; then
            mkdir -p $GITHUB_WORKSPACE/build/app/outputs/flutter-apk/
            cp "$APK" $GITHUB_WORKSPACE/build/app/outputs/flutter-apk/app-release.apk
          fi

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30

      - name: Create Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: Release v1.0.${{ github.run_number }}
          body: |
            Novel Writer v1.0.${{ github.run_number }}

            自动构建于 ${{ github.event.head_commit.timestamp }}
          files: build/app/outputs/flutter-apk/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}